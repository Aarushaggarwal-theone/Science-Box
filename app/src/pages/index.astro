---
// No need to import redirect, use Response.redirect instead

export const prerender = false;

export async function get() {
    return new Response(null, {
        status: 302,
        headers: {
            Location: '/home'
        }
    });
}
---